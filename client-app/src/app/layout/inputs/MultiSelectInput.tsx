import React from 'react';

interface Props {
    label: string 
    idName: string
    options: any[]
    disabled?: boolean | undefined
}

export default function MultiSelectInput({ label, idName, options, disabled }: Props){
    return (
        <></>
    )
}